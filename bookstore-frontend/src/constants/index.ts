/**
 * Constants
 *
 * Purpose: App-wide constant values (enums, magic numbers, static data).
 *
 * Tech Stack:
 * - TypeScript (const assertions, enums, or plain objects)
 *
 * Examples:
 * export const ROUTES = { HOME: '/', ADMIN: '/admin' } as const
 * export const MAX_FILE_SIZE = 5 * 1024 * 1024 // 5MB
 * export enum UserRole { ADMIN = 'ADMIN', USER = 'USER' }
 */

export {};
