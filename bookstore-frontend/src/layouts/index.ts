/**
 * Layouts
 *
 * Purpose: Layout wrapper components for different sections of the app.
 * - `admin/` — admin dashboard layout
 * - `user/` — user-facing layout
 *
 * Tech Stack:
 * - React 18+ with TypeScript
 * - React Router (if routing is added)
 * - May include navigation, sidebars, headers, footers
 *
 * Example:
 * export { AdminLayout } from './admin/admin-layout'
 * export { UserLayout } from './user/user-layout'
 */

export {};
