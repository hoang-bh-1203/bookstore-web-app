/**
 * UI Components (Design System Primitives)
 *
 * Purpose: Low-level, reusable design system components following shadcn/ui pattern.
 *
 * Tech Stack:
 * - React 18+ with TypeScript
 * - Radix UI headless primitives (@radix-ui/react-slot, etc.)
 * - class-variance-authority for variant-based styling
 * - Tailwind CSS with CSS variables from src/index.css
 * - lucide-react icons
 *
 * Components managed via:
 * - shadcn/ui CLI (components.json config)
 * - Style: "new-york"
 * - Base color: "neutral"
 *
 * Example:
 * export { Button, buttonVariants } from './button'
 */

export { Button, buttonVariants } from './button';
