/**
 * Common Components
 *
 * Purpose: Small, reusable components shared across the application.
 * Examples: Loaders, custom cards, badges, tooltips, etc.
 *
 * Tech Stack:
 * - React 18+ with TypeScript
 * - Tailwind CSS
 * - May compose ui/ primitives
 *
 * Pattern:
 * - Self-contained, reusable UI elements
 * - Can accept children and custom props
 * - Should be framework-agnostic where possible
 */

export {};
