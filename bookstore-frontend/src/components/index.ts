/**
 * Components
 *
 * Purpose: React component library organized by type.
 * - `common/` — small shared/reusable components (e.g., loaders, cards)
 * - `ui/` — design-system primitives following shadcn/ui pattern
 *
 * Tech Stack:
 * - React 18+ with TypeScript
 * - Radix UI primitives (@radix-ui/react-slot)
 * - class-variance-authority for variant management
 * - Tailwind CSS for styling
 * - lucide-react for icons
 *
 * Example:
 * import { Button } from '@/components/ui/button'
 * import { CustomCard } from '@/components/common/custom-card'
 */

export {};
