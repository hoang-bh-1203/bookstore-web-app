/**
 * Providers
 *
 * Purpose: React Context providers and wrapper components.
 *
 * Tech Stack:
 * - React Context API
 * - May wrap: Theme provider, Auth provider, Query provider
 * - TypeScript for type-safe contexts
 *
 * Example:
 * export { ThemeProvider } from './theme-provider'
 * export { AuthProvider } from './auth-provider'
 */

export {};
